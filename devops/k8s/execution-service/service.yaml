apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: codeview
  labels:
    app: execution-service
spec:
  type: ClusterIP
  selector:
    app: execution-service
  ports:
    - name: http
      protocol: TCP
      port: 5001
      targetPort: 5001
