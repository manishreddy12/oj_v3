apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: codeview
  labels:
    app: api-service
spec:
  type: ClusterIP          # Ingress handles external access
  selector:
    app: api-service
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
