FROM ubuntu:22.04

# Install only the compilers needed for code execution
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
      g++ \
      gcc \
      python3 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /code

# No CMD — this image is used with `docker run ... command`
